{
  "name": "jamii",
  "version": "1.0.0",
  "private": true,
  "description": "JamiiChat — a social platform with live conversations, built with the MERN stack and Socket.IO",
  "author": {
    "name": "David Mwendwa",
    "url": "https://github.com/David-mwendwa"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npx concurrently --kill-others-on-fail --names \"FRONTEND,BACKEND\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "seed": "npm run seed --prefix backend",
    "test:realtime": "npm run test:realtime --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
